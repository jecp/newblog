ul.pager
  if (!isFirstPage)
    li.previous
      a(class="prepage",title="上一页",href="?p=#{page-1}") &larr;上一页
  if (!isLastPage)
    li.next
      a(class="nextpage",title="下一页",href="?p=#{page+1}") &rarr;下一页